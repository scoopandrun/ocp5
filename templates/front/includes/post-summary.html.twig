<!-- Post preview-->
<div class="post-preview">
	<a href="/posts/{{ post.id }}">
		<h2 class="post-title">{{ post.title }}</h2>
		<h3 class="post-subtitle">{{ post.leadParagraph }}</h3>
	</a>
	<p class="post-meta">
		Posté par
		{{ post.author.name ?? "Anonyme" }}
		le
		{{ post.createdAt|format_date("full", locale="fr") }}
		{% if post.category %}
			dans
			{{ post.category }}
		{% endif %}
	</p>
</div>
